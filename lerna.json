{
  "ci": false,
  "packages": [
    "packages/*"
  ],
  "version": "0.6.5",
  "command": {
    "bootstrap": {
      "hoist": true
    },
    "publish": {
      "allowBranch": "main",
      "message": "chore(release): publish %s",
      "conventionalCommits": true,
      "createRelease": "github"
    }
  },
  "ignoreChanges": [
    "packages/*"
  ]
}
